import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

ReactDOM.render(<App />, document.getElementById("root"));

// challenge: to pick the fname and the lname and update it at the header.

// 1.capture the data input by the user.

// 2. Render the data as separate values on the header.
